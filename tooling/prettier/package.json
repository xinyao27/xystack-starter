{
  "name": "@xystack/prettier-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.js"
  },
  "scripts": {
    "clean": "git clean -xdf .cache .turbo node_modules",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "prettier": "catalog:"
  },
  "devDependencies": {
    "@xystack/tsconfig": "workspace:*",
    "typescript": "catalog:"
  },
  "prettier": "@xystack/prettier-config"
}
